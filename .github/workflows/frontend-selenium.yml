# Run one frontend Selenium test on every push (fast smoke check)
name: Frontend Selenium (tcid67)

on:
  push:
    branches: [main, 'ci/**', 'feature/*']

jobs:
  frontend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Check Chrome version
        run: |
          google-chrome --version
          chromedriver --version

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .

      - name: Run frontend test (tcid67 – verify heading)
        env:
          BROWSER: headlesschrome
          RESULTS_DIR: ./results
          ENV: test
          API_KEY: ck_ci_placeholder
          API_SECRET: cs_ci_placeholder
          DB_USER: root
          DB_PASSWORD: ci_placeholder
        run: |
          mkdir -p results
          pytest ssqatest/tests -v
